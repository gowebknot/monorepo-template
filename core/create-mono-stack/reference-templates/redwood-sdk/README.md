# RedwoodSDK reference

Marker: `redwood-sdk-reference`

This is code-only reference material. Import `ReferencePage` and register it with the native worker's existing `render()` and `route()` tree. The launcher does not add or replace RedwoodSDK, Cloudflare, Vite, or worker configuration, dependencies, or scripts.
