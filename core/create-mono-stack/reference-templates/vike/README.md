# Vike reference

Marker: `vike-reference`

This is code-only reference material. Copy the `pages/reference` directory into the native Vike page tree to expose `/reference`. The launcher does not add or replace Vike renderer or Vite configuration, dependencies, or scripts.
