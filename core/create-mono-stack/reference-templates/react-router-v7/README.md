# React Router v7 reference

Marker: `react-router-v7-reference`

This is code-only reference material. Copy or register the route module from `app/routes/reference.tsx` in the native app's `app/routes.ts`. The launcher does not add or replace React Router configuration, dependencies, or scripts.
