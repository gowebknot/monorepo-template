# React Native has no built-in env inlining. These values seed src/config.ts by
# convention; wire a native loader (e.g. react-native-config) for real overrides.
RN_PUBLIC_APP_URL=http://localhost:8081
RN_PUBLIC_API_BASE_URL=http://localhost:3001
