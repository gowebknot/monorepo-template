# TanStack Router reference

Marker: `tanstack-router-reference`

This is code-only reference material. Copy `src/routes/reference.tsx` into the native app's route tree and let its existing TanStack Router tooling regenerate route metadata. The launcher does not add or replace router configuration, dependencies, or scripts.
