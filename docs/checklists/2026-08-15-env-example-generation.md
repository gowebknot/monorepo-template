# Environment Example Generation

- Checklist ID: CHECKLIST-20260815-env-example-generation
- Created: 2026-08-15
- Planning completed: 2026-08-15
- Type: Feature and generated-project configuration
- Source request: Add `.env.example` to the template and create a matching ignored `.env` in new
  consumer projects generated by the launcher.
- Related checklists:
  - [Complete Hybrid Native Reference Generation](./2026-08-14-complete-hybrid-native-reference-generation.md)
- Affected paths: `.env.example`, `copier.yml`, `core/create-mono-stack/src/create-project.js`,
  launcher tests, generated-project documentation
- Status legend: `[ ]` incomplete, `[/]` partial, `[x]` complete

## Planning Record

- [x] Read repository, environment-package, launcher, Copier, and security guidance.
- [x] Confirmed no committed `.env` or `.env.example` currently exists.
- [x] Confirmed `.env` is ignored and Copier excludes it, while `.env.example` is not excluded.
- [x] Defined safe non-secret defaults from `packages/env/src/global-env.ts`.
- [x] Split source-file, launcher-copy, update-preservation, failure-cleanup, and documentation cases.

## Acceptance Criteria

- [x] The template contains a committed `.env.example` with safe local defaults.
- [x] New launcher-generated projects contain `.env.example` and a matching `.env`.
- [x] Generated `.env` remains ignored and is never committed by launcher Git initialization.
- [x] Template updates preserve the consumer's existing `.env` while allowing `.env.example` updates.
- [x] Setup failures remove the generated environment files with the partial project.
- [x] Documentation explains the example and ignored local file behavior.

## Exact Test Cases

- [x] TEST-ENV-001: The template environment example lists the supported safe defaults.
  - Small task: Add the source environment example.
  - Source: `packages/env/src/global-env.ts`.
  - Test place: Exact text test in `core/create-mono-stack/test/copier-template.test.js`.
  - Starting state: No root `.env.example` exists.
  - Exact input or fixture: The six documented local values for web, API, database, port, reference
    port, and allowed origins.
  - Interaction steps: Read `.env.example` and compare its complete contents.
  - Main behavior: Provide a safe copyable environment template.
  - Expected result: Every supported variable appears with a non-secret local default.
  - Must change: `.env.example` is added and tracked.
  - Must not happen: Credentials, tokens, production URLs, or `.env` are added.
  - Planned command: `node --test --test-name-pattern='TEST-ENV-001' core/create-mono-stack/test/copier-template.test.js`.
  - Expected result before the code change: The test fails because `.env.example` is missing.
  - First observed run: Focused test passed after adding the source example.
  - Passing rerun: `pnpm --filter create-mono-stack test` passed.

- [x] TEST-ENV-002: New project creation copies the example into an ignored `.env`.
  - Small task: Bootstrap consumer environment files after Copier rendering.
  - Source: User request and `createProject` generation order.
  - Test place: Launcher unit test in `core/create-mono-stack/test/create-project-native.test.js`.
  - Starting state: Copier has rendered `.env.example`; the destination is an empty new project.
  - Exact input or fixture: The exact six-line `.env.example` content.
  - Interaction steps: Run `createProject` with controlled filesystem dependencies and inspect writes.
  - Main behavior: Create `.env` from the rendered example before setup completes.
  - Expected result: `.env` content equals `.env.example`, and the copy occurs after Copier rendering.
  - Must change: One `.env` file is created in the destination.
  - Must not happen: Secrets are generated, shell commands are used, or `.env.example` is deleted.
  - Planned command: `node --test core/create-mono-stack/test/create-project-native.test.js`.
  - Expected result before the code change: The test fails because no `.env` copy event exists.
  - First observed run: Focused test passed after injecting the copy dependency.
  - Passing rerun: `pnpm --filter create-mono-stack test` passed.

- [x] TEST-ENV-003: Template update does not overwrite an existing consumer `.env`.
  - Small task: Preserve local environment values during updates.
  - Source: `copier.yml` exclusion of `.env` and generated-project safety rules.
  - Test place: Copier integration assertion in `core/create-mono-stack/test/copier-template.integration.test.js`.
  - Starting state: Generated project has a customized `.env`; template update changes `.env.example`.
  - Exact input or fixture: Consumer marker `DATABASE_URL=consumer-secret-placeholder` and updated
    example marker.
  - Interaction steps: Write the consumer `.env`, update the template, then read both files.
  - Main behavior: Preserve local environment while updating the example.
  - Expected result: `.env` retains the consumer marker and `.env.example` receives the template marker.
  - Must change: Only `.env.example` changes.
  - Must not happen: `.env` is not overwritten or committed.
  - Planned command: `pnpm --filter create-mono-stack test:integration`.
  - Expected result before the code change: The integration has no environment-file assertions.
  - First observed run: Integration initially failed because the consumer marker was added before the baseline fixture assertion.
  - Passing rerun: `pnpm --filter create-mono-stack test:integration` passed with `.env` preservation and `.env.example` update assertions.

- [x] TEST-ENV-004: Setup failure removes both environment files with the partial project.
  - Small task: Preserve cleanup behavior after environment bootstrap.
  - Source: Existing launcher cleanup contract in `project-cleanup.js` and `create-project.js`.
  - Test place: Failure test in `core/create-mono-stack/test/cli.test.js`.
  - Starting state: Copier creates `.env.example`; environment bootstrap creates `.env`; final lockfile
    generation fails.
  - Exact input or fixture: Synthetic `Registry unavailable` lockfile error.
  - Interaction steps: Run creation, fail the lockfile command, and inspect the destination and temporary
    root.
  - Main behavior: Remove partial project state after failure.
  - Expected result: Destination, `.env`, `.env.example`, and temporary root are removed.
  - Must change: Cleanup events include both environment files through destination removal.
  - Must not happen: Persistent Python environment or Git initialization runs after failure.
  - Planned command: `node --test core/create-mono-stack/test/cli.test.js`.
  - Expected result before the code change: Existing cleanup passes but does not prove environment files.
  - First observed run: Existing cleanup test passed but did not record the environment copy.
  - Passing rerun: `pnpm --filter create-mono-stack test` passed with the copy-before-failure assertion and recursive destination cleanup.

- [x] TEST-ENV-005: Documentation explains the committed example and ignored local file.
  - Small task: Document generated environment setup.
  - Source: User request and environment package defaults.
  - Test place: Documentation assertions in `core/create-mono-stack/test/copier-template.test.js`.
  - Starting state: README files do not mention `.env.example` or generated `.env` creation.
  - Exact input or fixture: Root and launcher README sources.
  - Interaction steps: Read the documentation and assert both filenames and local-default guidance.
  - Main behavior: Tell consumers how to customize local environment values safely.
  - Expected result: Documentation says `.env.example` is committed/copied and `.env` is local/ignored.
  - Must change: Root and launcher documentation are updated.
  - Must not happen: Documentation must not suggest committing `.env` or include secrets.
  - Planned command: `node --test --test-name-pattern='TEST-ENV-005' core/create-mono-stack/test/copier-template.test.js`.
  - Expected result before the code change: The test fails because the environment-file guidance is absent.
  - First observed run: Documentation test initially failed because Prettier wraps the Markdown code span across a newline.
  - Passing rerun: `pnpm --filter create-mono-stack test` passed with whitespace-tolerant assertions.

- [x] TEST-ENV-006: A custom template without `.env.example` reports an actionable setup error.
  - Small task: Clarify the launcher failure when a template cannot provide the environment example.
  - Source: Active checklist follow-up and the launcher environment-copy boundary.
  - Test place: Launcher unit test in `core/create-mono-stack/test/create-project-native.test.js`.
  - Starting state: Copier completes, but the custom template has no `.env.example`.
  - Exact input or fixture: An `ENOENT` error from the injected environment copy operation.
  - Interaction steps: Run `createProject`, reject the copy, and inspect the returned error.
  - Main behavior: Explain that the template must provide `.env.example`.
  - Expected result: Setup rejects with an error naming `.env.example` and the destination.
  - Must change: The failure message is actionable and preserves the existing cleanup path.
  - Must not happen: Setup must not continue to native scaffolding or lockfile generation.
  - Planned command: `node --test --test-name-pattern='TEST-ENV-006' core/create-mono-stack/test/create-project-native.test.js`.
  - Expected result before the code change: The test fails because the raw missing-file error is not actionable.
  - First observed run: Failed with the raw `missing` error, confirming the message was not actionable.
  - Passing rerun: `node --test --test-name-pattern='TEST-ENV-006' core/create-mono-stack/test/create-project-native.test.js` passed with the template path in the error.

## Implementation Plan

- [x] Add `.env.example` with values matching the environment schema.
- [x] Copy the rendered example to `.env` using Node filesystem APIs after Copier rendering.
- [x] Add focused unit, integration, cleanup, and documentation assertions.
- [x] Update README sources without exposing secrets.
- [x] Run focused tests, package tests, integration, formatting, and repository checks.

## Verification

- [x] Run every exact test case and record first and passing results.
- [x] Run `pnpm --filter create-mono-stack test`.
- [x] Run `pnpm --filter create-mono-stack lint`.
- [x] Run `pnpm --filter create-mono-stack test:integration`.
- [x] Run `just check` and `git diff --check`.
- [x] Confirm `.env` remains ignored and no secrets are present.

## Risks and Follow-Up

- [x] A custom template without `.env.example` now fails clearly instead of continuing with incomplete
      environment setup.
- [x] Existing consumer projects need a separate migration or manual rerun; this change targets new
      projects created by the launcher.
